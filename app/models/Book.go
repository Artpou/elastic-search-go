package models

type Book struct {
	title  string
	author string
	//abstract string
	year string
}

/* func NewBook(title string, author string, abstract string, year string) *Book {
	book := Book{Title: title, Author: author, Abstract: abstract, Year: year}
	return &book
} */
